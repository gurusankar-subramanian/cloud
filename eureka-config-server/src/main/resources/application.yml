spring:
   application:
     name: config-server
   cloud:
     config:
      server:
        git:
          uri: https://github.com/gurusankar-subramanian/cloud-central-repo

server:
       port: 999

management:
   security:
       enabled: false